name: '🏗️ Build Plugin'

on:
  push:
    branches:
      - master
    tags-ignore:
      - '**'  # Don't run on tag pushes (publish.yml handles releases)
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches:
      - master
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/build.yaml@master
